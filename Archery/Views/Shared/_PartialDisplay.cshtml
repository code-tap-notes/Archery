@{   
    var message = TempData["MESSAGE"] as Archery.Validators.Message;

}
@if (message != null)
{
    <div class="alert alert-@(message.MessageType == Archery.Validators.MessageType.Success ? "success" : "danger")">
        @message.Text
    </div>
}

